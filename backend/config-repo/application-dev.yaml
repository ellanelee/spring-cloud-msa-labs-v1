spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

    logging:
      level:
        com.sesac: DEBUG
        org.springframework.web: DEBUG
        org.hibernate.SQL: DEBUG

    info: #custom field추가설정
      environment: "Development"
      debug: true